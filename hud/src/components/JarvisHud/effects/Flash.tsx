/** Bloom flash overlay — pulsed by the .jhud.firing class from the parent
 *  during the shutter-fire animation. The opacity keyframes live in JarvisHud.css. */
export function Flash() {
  return <div className="flash" />;
}
