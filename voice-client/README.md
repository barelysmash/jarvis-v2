# JARVIS voice client (rosencrantz)

Canonical copy of `C:\Users\bgama\jarvis-voice\voice_client.py`.
The LIVE copy runs from `~/jarvis-voice/` with its own `.env` (keys are NOT
in this repo — see the docstring in voice_client.py for the variable list).

Deploy = copy this file over `~/jarvis-voice/voice_client.py` and relaunch.
Keep the two in sync: patch here first, then copy out.
